.page-header
  = link_to new_f_fight_url, class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-plus
    = t( '.new' )
  %h1= t( '.list' )

.table-responsive
  %table.table.table-striped.table-bordered.table-hover
    %thead
      %tr
        %th= t( 'c_combat.result' ).capitalize
        %th
        %th
        %th

    %tbody
      - @f_fights.each do |c_combat|
        %tr
          %td= c_combat.id
          %td= link_to t( 'show' ), c_combat
          %td= link_to t( 'edit' ), edit_f_fight_url(c_combat)
          %td= link_to t( 'destroy' ), c_combat, data: { confirm: 'Are you sure?' }, method: :delete
