.page-header
  = link_to c_combats_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    =t('back')
  = link_to edit_c_combat_path(@c_combat), class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-pencil
    =t('edit')
  %h1= t('.show')

%dl.dl-horizontal
  %dt=t('c_combat.result').capitalize+':'
  %dd= @c_combat.result
