.page-header
  = link_to <%= index_helper %>_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    = t('back')
  %h1= t( 'new_'+t('<%= singular_table_name %>_gender') ) + ' ' + t( '<%= singular_table_name %>' )

= render 'form'
